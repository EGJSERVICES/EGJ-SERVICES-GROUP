<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="description" content="EGJ Services Group – Leading trucking services in South Florida. We specialize in three-axle truck transportation, hauling aggregates, and more." />
  <meta name="keywords" content="trucking, construction, dump truck, hauling, aggregates, South Florida" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>EGJ Services Group | Reliable Trucking Services</title>
  <!-- External Stylesheets -->
  <link rel="stylesheet" href="styles.css" />
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <!-- jsPDF for PDF generation -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
  <script defer src="script.js"></script>
  <style>
    /* ===== CSS Variables for Theme Colors ===== */
    :root {
      --bg-color: #f5f5f5;
      --text-color: #333;
      --nav-bg: #f0f0f0;
      --nav-border: #add8e6;
      --btn-bg: #007BFF;
      --btn-hover-bg: #0056b3;
      --content-bg: #ffffff;
      --header-bg: rgba(255, 255, 255, 0.95);
      --modal-bg: #ffffff;
      --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    body.dark-mode {
      --bg-color: #121212;
      --text-color: #f0f0f0;
      --nav-bg: #222;
      --nav-border: #444;
      --btn-bg: #1E90FF;
      --btn-hover-bg: #1C86EE;
      --content-bg: #1e1e1e;
      --header-bg: rgba(18, 18, 18, 0.95);
      --modal-bg: #1e1e1e;
      --shadow: 0 4px 6px rgba(255, 255, 255, 0.1);
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }
    body {
      background-color: var(--bg-color);
      color: var(--text-color);
      transition: background-color 0.3s, color 0.3s;
    }
    header {
      background: var(--header-bg);
      padding: 1rem;
      text-align: center;
      font-size: 1.5rem;
      box-shadow: var(--shadow);
    }
    nav {
      background: var(--nav-bg);
      padding: 1rem;
      text-align: center;
      border-bottom: 2px solid var(--nav-border);
    }
    .container {
      max-width: 1100px;
      margin: auto;
      padding: 20px;
      background: var(--content-bg);
      box-shadow: var(--shadow);
      border-radius: 10px;
    }
    .btn {
      display: inline-block;
      padding: 10px 20px;
      background: var(--btn-bg);
      color: white;
      border: none;
      cursor: pointer;
      transition: background 0.3s;
      border-radius: 5px;
    }
    .btn:hover {
      background: var(--btn-hover-bg);
    }
  </style>
</head>
<body>
  <header>
    EGJ Services Group – Professional Trucking Services
  </header>
  <nav>
    <a href="#" class="btn">Home</a>
    <a href="#about" class="btn">About</a>
    <a href="#services" class="btn">Services</a>
    <a href="#contact" class="btn">Contact</a>
  </nav>
  <div class="container">
    <section id="about">
      <h2>About Us</h2>
      <p>EGJ Services Group specializes in three-axle truck transportation, hauling aggregates, and construction material transportation across South Florida.</p>
    </section>
    <section id="services">
      <h2>Our Services</h2>
      <ul>
        <li>Aggregate Hauling</li>
        <li>Construction Material Transport</li>
        <li>Dump Truck Services</li>
      </ul>
    </section>
    <section id="contact">
      <h2>Contact Us</h2>
      <p>Email: info@egjservices.com</p>
      <p>Phone: (123) 456-7890</p>
    </section>
  </div>
</body>
</html>
